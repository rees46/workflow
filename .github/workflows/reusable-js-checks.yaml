name: Reusable JS/TS PR checks

on:
  workflow_call:
    inputs:
      node-version:
        description: 'Node version to run this workflow. Default: 18 as it is in action cache'
        default: '18'
        required: false
        type: string
      workingDirectory:
        description: Working direction for action
        default: ./
        type: string
        required: false

jobs:
  run:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ${{ inputs.workingDirectory }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: ${{ inputs.node-version }}

      - name: Switch branch
        run: git switch ${{ github.head_ref }}

      - name: Install
        run: yarn install --inline-builds

      - name: Lint
        env:
          TARGET: ${{ github.base_ref }}
        run: yarn workspaces foreach --since=origin/$TARGET run lint

      - name: Test
        env:
          TARGET: ${{ github.base_ref }}
        run: yarn workspaces foreach --since=origin/$TARGET run test

      - name: Build
        env:
          TARGET: ${{ github.base_ref }}
        run: yarn workspaces foreach --since=origin/$TARGET run build
